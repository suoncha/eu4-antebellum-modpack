 country_decisions = {
	
	aba_gal_release_celt_decision = {
 		major = yes
		potential = {
			ai = no
			has_country_flag = aba_gal_release_celt
			NOT = { exists = CLT }
		}
		allow = {
		}
		effect = {
			clr_country_flag = aba_gal_release_celt
			custom_tooltip = aba_release_celt_tt
			hidden_effect = {
				british_isles_region = {
					limit = {
						owned_by = ROOT
					}
					add_core = CLT
					remove_core = ROOT
				}
				release = CLT
				create_union = CLT
				CLT = {
					set_capital = 234
					set_government_rank = 2
					add_government_reform = feudalism_reform
					add_manpower = 10
 					add_treasury = 100
					add_liberty_desire = -50
					change_religion = ROOT
					change_primary_culture = baltic_german
					capital_scope = {
						change_religion = ROOT
					}
				}
			}
 		}
		ai_will_do = {
			factor = 0
		}
	}
}